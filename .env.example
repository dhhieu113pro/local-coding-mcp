# Copy to .env in this same folder:
#   copy .env.example .env
# Do NOT commit .env

# === Host code folder (forward slashes on Windows) ===
MCP_WORKSPACE=D:/wslc/workspaces
# MCP_WORKSPACE=/home/you/projects

# === Codebase Memory MCP (optional profile) ===
# Start: docker compose --profile codebase-memory up -d
CODEBASE_MEMORY_VERSION=v0.10.8
SUPERGATEWAY_VERSION=3.2.0
CBM_PORT=9750
CBM_DATA=./codebase-memory
CBM_MEMORY_LIMIT=1g
CBM_CPU_LIMIT=2.0
# LocalCodingMcp proxies the sidecar so clients can keep a single /mcp connection.
CODEBASE_MEMORY_PROXY_ENABLED=true
CODEBASE_MEMORY_PROXY_ENDPOINT=http://codebase-memory:9750/mcp
CODEBASE_MEMORY_PROXY_TIMEOUT_SECONDS=15

# === ngrok (public HTTPS for ChatGPT) ===
# https://dashboard.ngrok.com/get-started/your-authtoken
# Start:
#   docker compose up -d
#   docker compose --profile ngrok up -d
NGROK_AUTHTOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# === Optional IDE ===
# docker compose --profile ide up -d
CODE_SERVER_PASSWORD=changeme
