# LocalCodingMcp + OpenAI tunnel-client (shared network)
#
# Setup:
#   1. Copy .env.example → .env and fill secrets
#   2. docker compose up -d
#
# Windows MCP_WORKSPACE example: D:/wslc/workspaces
# WSL example:                   /home/you/projects

services:
  local-coding-mcp:
    image: ghcr.io/dhhieu113pro/local-coding-mcp:latest
    container_name: local-coding-mcp
    ports:
      - "5000:5000"
    environment:
      ASPNETCORE_URLS: "http://0.0.0.0:5000"
      ASPNETCORE_ENVIRONMENT: Production
      AllowedRoots__0: /workspace
      CommandTimeoutSeconds: "60"
      MaxSearchResults: "100"
    volumes:
      # Host folder → /workspace inside container
      - ${MCP_WORKSPACE:-./workspace}:/workspace
    networks:
      - mcp-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:5000/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

  tunnel-client:
    image: ghcr.io/openai/tunnel-client:latest
    container_name: local-coding-mcp-tunnel
    depends_on:
      local-coding-mcp:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      CONTROL_PLANE_API_KEY: ${CONTROL_PLANE_API_KEY}
      CONTROL_PLANE_TUNNEL_ID: ${CONTROL_PLANE_TUNNEL_ID}
      # Same compose network → use service name (not localhost / host.docker.internal)
      MCP_SERVER_URL: http://local-coding-mcp:5000/mcp
      LOG_LEVEL: info
      LOG_FORMAT: json
      HEALTH_LISTEN_ADDR: ":8080"
    networks:
      - mcp-net
    restart: unless-stopped

  # Optional browser IDE — only with: docker compose --profile ide up -d
  code-server:
    profiles: ["ide"]
    image: codercom/code-server:latest
    container_name: local-coding-mcp-ide
    environment:
      PASSWORD: ${CODE_SERVER_PASSWORD:-changeme}
    volumes:
      - ${MCP_WORKSPACE:-./workspace}:/home/coder/project
    ports:
      - "8443:8080"
    command:
      - --bind-addr
      - 0.0.0.0:8080
      - --auth
      - password
      - /home/coder/project
    networks:
      - mcp-net
    restart: unless-stopped

networks:
  mcp-net:
    name: mcp-net
    driver: bridge
